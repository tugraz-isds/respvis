import {Meta} from "@storybook/blocks";

<Meta title='CSS Caveats'/>

TODO: Describe typical css caveats when working with respvis like
- when using container queries in HTML tree, svg styles can't be changed as svg elements are not in html tree
- respvis handling of svg text baseline under the hood and the difficulties
- layout breakpoints are only available since window-rv class as it is outest container of generated respvis structure.


