import {Meta} from "@storybook/blocks";
import {Tooltip} from "../util/documentation-components/tooltip-text";
import {modulesMap} from "../../../../gulp-tasks/constants/modules";
import {projectAbsolutePaths} from "../../../../gulp-tasks/paths/project-absolute-paths";

<Meta title='RespVis Packages'/>

# RespVis Package and Modules

The source code of RespVis consists of multiple typescript modules:

- <Tooltip text={projectAbsolutePaths.modulesPaths["respvis-core"]}>***{modulesMap["respvis-core"]}***</Tooltip>:
  Contains the core functionality of RespVis, including
- <Tooltip text={projectAbsolutePaths.modulesPaths["respvis-cartesian"]}>***{modulesMap["respvis-cartesian"]}***</Tooltip>:
  Contains
- <Tooltip text={projectAbsolutePaths.modulesPaths["respvis-bar"]}>***{modulesMap["respvis-bar"]}***</Tooltip>:
  Contains
- <Tooltip text={projectAbsolutePaths.modulesPaths["respvis-point"]}>***{modulesMap["respvis-point"]}***</Tooltip>:
  Contains
- <Tooltip text={projectAbsolutePaths.modulesPaths["respvis-line"]}>***{modulesMap["respvis-line"]}***</Tooltip>:
  Contains
- <Tooltip text={projectAbsolutePaths.modulesPaths["respvis-tooltip"]}>***{modulesMap["respvis-tooltip"]}***</Tooltip>:
  Contains


TODO: Explain:
- importmap + how to use packages without npm (standalone vs. dependency based)
- css structuring
- structure of repo once finally decided (monorepo, mulitple packages, npm workspaces, gulp, storybook)
